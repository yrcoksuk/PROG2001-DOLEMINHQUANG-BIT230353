#include <stdio.h>

int main() {
    float pi = 3.14159;

    printf("Mac dinh (6 chu so thap phan): %f\n", pi);
    printf("1 chu so thap phan:            %.1f\n", pi);
    printf("4 chu so thap phan:            %.4f\n", pi);

    return 0;
}