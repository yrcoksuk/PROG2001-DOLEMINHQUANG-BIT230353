#include <stdio.h>

int main() {
    int n;
    printf("Nhap n: ");
    scanf("%d", &n);

    int S = 0;
    for (int i = 1; i <= n; i++) {
        S += i;   // S = S + i
    }

    printf("S = 1 + 2 + ... + %d = %d\n", n, S);
    return 0;
}